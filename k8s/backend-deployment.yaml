apiVersion: apps/v1
kind: Deployment
metadata:
  name: cask-and-cup-backend
  namespace: cask-and-cup
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cask-and-cup-backend
  template:
    metadata:
      labels:
        app: cask-and-cup-backend
      annotations:
        reloader.stakater.com/auto: "true"
    spec:
      containers:
        - name: api
          image: ghcr.io/wisdomwolf/cider-adventure-log-api:sha-22c43af
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "production"
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: cask-and-cup-secrets
                  key: SECRET_KEY
            - name: FLASK_DB_URI
              valueFrom:
                secretKeyRef:
                  name: cask-and-cup-secrets
                  key: FLASK_DB_URI
            - name: AUTHENTIK_ISSUER
              value: "https://authentik.developingwisdom.org/application/o/cask-and-cup/"
            - name: AUTHENTIK_REDIRECT_URI
              value: "https://caskandcup.developingwisdom.org/api/auth/sso/callback"
            - name: AUTHENTIK_AUTO_PROVISION_GROUP
              value: "Cask & Cup Users"
            - name: AUTHENTIK_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: cask-and-cup-secrets
                  key: AUTHENTIK_CLIENT_ID
            - name: AUTHENTIK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: cask-and-cup-secrets
                  key: AUTHENTIK_CLIENT_SECRET
          livenessProbe:
            httpGet:
              path: /api/beverages
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/beverages
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cask-and-cup-backend
  namespace: cask-and-cup
spec:
  selector:
    app: cask-and-cup-backend
  ports:
    - port: 5000
      targetPort: 5000
